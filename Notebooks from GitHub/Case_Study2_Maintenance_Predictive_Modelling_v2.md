# Technical Documentation: Predicting Optimal Machine Maintenance Cycle

## 1. Notebook Overview
Problem statement (from the notebook's own description): In this study, the author investigates predicting optimal machine maintenance cycle. The problem is explicitly treated as a predictive modelling problem where the primary target feature is identified as the `Maintenance` feature.

Overall analytical approach as shown in the notebook: The workflow begins with Exploratory Data Analysis (EDA) on a dataset consisting of precisely 600 rows. The notebook states the dataset is in a clean state with no missing values or outliers, hence the lack of any data pre-processing efforts beyond dropping an ID column. The analytical approach relies heavily on establishing a consistent train-test split consisting of 70% training data and 30% testing data. Three specific machine learning models are fit over the course of the analysis:
- A Logistic Regression model trained utilizing all of the available descriptive features.
- A Logistic Regression model trained utilizing only the single `Count_C` feature.
- A Decision Tree model trained utilizing only the single `Count_C` feature.
Model performance is evaluated and compared using simple accuracy metrics as well as the Area Under the Receiver Operating Characteristic Curve (AUC) metric. Finally, a proof-of-concept deployment simulation is conducted by saving the model to disk, loading it back into memory, and executing a prediction on a mocked JSON input observation.

Notable characteristics of the workflow: The workflow heavily relies on visualizations to uncover relationships and linear separability between variables before any mathematical modelling takes place. High collinearity between features (specifically between Speed and Pressure) results in a deliberate decision to drop a feature prior to any machine learning algorithm being applied. Furthermore, the notebook actively contrasts a more complex model (using all features) against simple models (using one feature) to justify and arrive at a highly parsimonious final model consisting of a depth-1 decision tree (effectively a decision stump).

## 2. Environment and Dependencies
All libraries imported and their specific roles as utilized in this notebook:
- `warnings`: Imported to explicitly filter and ignore warning messages generated by Python or external packages using `warnings.filterwarnings("ignore")`.
- `numpy` (`np`): Utilized for fundamental array manipulation, specifically generating 300 linearly spaced values using `np.linspace` and reshaping arrays with `.reshape(-1, 1)` for predictive modelling plotting and deployment inference.
- `pandas` (`pd`): Utilized for core data manipulation, reading CSV data into a DataFrame (`pd.read_csv`), displaying options (`pd.set_option('display.max_columns', None)`), calculating the sum of missing values (`isnull().sum()`), describing numerical data (`describe()`), computing value counts for distributions (`value_counts()`), generating correlation matrices (`.corr()`), and permanently dropping columns (`drop()`). It is also used to construct a DataFrame for ROC curve line plotting.
- `matplotlib.pyplot` (`plt`): Utilized for creating and customizing visualizations, setting figure sizes, adding titles, x-labels, y-labels, legends, annotations (such as arrows and text), and rendering plots to the screen (`plt.show()`). Specific usage of `plt.style.use("seaborn-v0_8")` sets the global aesthetic styling of the charts.
- `seaborn` (`sns`): Utilized for high-level statistical data visualization, specifically rendering a correlation `heatmap`, drawing bar chart `countplot` graphs, generating `boxplot` distributions, and rendering `scatterplot` distributions colored by target variables.
- `requests`: Utilized to perform HTTP GET requests to programmatically fetch the raw dataset from a remote GitHub repository URL.
- `io`: Utilized to provide string I/O (`io.StringIO`) capabilities in order to parse the raw byte content returned by the `requests` module directly into a pandas DataFrame without writing to local disk.
- `sklearn.model_selection.train_test_split`: Utilized to randomly split the dataset into training (70%) and testing (30%) matrices, utilizing the `stratify` parameter to rigorously maintain the class balance of the target variable across both splits.
- `sklearn.linear_model.LogisticRegression`: Utilized to instantiate, fit on training data, and score test data for logistic regression models.
- `sklearn.tree.DecisionTreeClassifier`: Utilized to instantiate, fit, and score test data for decision tree classification models.
- `sklearn.tree.plot_tree`: Utilized to generate a direct visual diagram of the internal node structure of the trained decision tree classifier.
- `sklearn.metrics`: Utilized for granular model evaluation, specifically extracting the `confusion_matrix` to evaluate distinct classification errors, extracting `roc_curve` arrays to calculate false positive and true positive rates, and utilizing `auc` to calculate the final scalar area under the ROC curve.
- `joblib`: Utilized to compress and serialize (using `.dump()`) the trained decision tree model object to a physical file named `DT.pkl` and deserialize (using `.load()`) it back into active memory for production-style predictions.

## 3. Per-Stage Documentation

### Preliminaries and Data Loading
**[CODE]** The notebook begins in the first code cells by importing `warnings`, `numpy`, `pandas`, `matplotlib.pyplot`, and `seaborn`. It explicitly sets matplotlib to use the "seaborn-v0_8" style and sets pandas to display an unlimited number of columns. It then imports `requests` and `io`, defines a URL string pointing to a raw CSV file on GitHub, fetches the remote content using `requests.get(df_url, verify=False).content`, and reads the decoded string into a pandas DataFrame object named `df` using `pd.read_csv(io.StringIO(...))`. Following this exact ingestion, the author uses slicing to drop the first column (an ID column) using `df = df.iloc[:, 1:]` and prints the first five rows using `df.head()`.
**[PROCESS/CONTEXT]** The author imports necessary libraries for data processing and visualization. Data is loaded directly from a remote URL to ensure reproducibility without requiring a local file download. The first column is dropped because it is an ID column, which does not hold predictive power.
**[RESULT]** The `df.head()` output displays a formatted HTML table consisting of 5 rows and 6 columns: `PRESSURE`, `BARREL_TEMP`, `SPEED`, `MODE`, `MAINTENANCE`, and `COUNT_C`. The first row explicitly contains the numeric values 91.76, 93.81, 47.92, 0, 0, and 17, respectively.
**[INSIGHT]** The data loading mechanism is highly robust as it fetches data programmatically over the network. Dropping the ID column immediately is an appropriate standard practice to prevent arbitrary unique identifiers from improperly influencing subsequent machine learning algorithms.

### Exploratory Data Analysis: Missing Values and Data Types
**[CODE]** The code prints the literal sum of missing values for every single column using `df.isnull().sum()`. It then samples exactly 5 random rows using `df.sample(5, random_state=999)` and sequentially displays the internal data types of the DataFrame using `df.dtypes`.
**[PROCESS/CONTEXT]** These procedural steps serve to rigorously confirm the notebook's initial introductory claim that the dataset is clean and genuinely has no missing values. Sampling the data provides a view of the dataset's structural layout, and checking data types ensures they exactly match intended numeric formats.
**[RESULT]** The stream output literally confirms `0` missing values across all 6 features. The data types output confirms they are correctly inferred by pandas: `PRESSURE`, `BARREL_TEMP`, and `SPEED` are represented as `float64`, while `MODE`, `MAINTENANCE`, and `COUNT_C` are represented as `int64`. The sample output displays randomly selected rows (specifically indexes 428, 566, 511, 103, and 86).
**[INSIGHT]** The dataset is indeed perfectly clean as initially claimed by the author. The deliberate use of a fixed integer `random_state=999` in the random sample method ensures deterministic, reproducible output across sequential runs of the notebook.

### Exploratory Data Analysis: Summary Statistics
**[CODE]** The code generates and displays a standard statistical 5-number summary utilizing the command `df.describe(include=['int64','float64']).T.round(2)`.
**[PROCESS/CONTEXT]** This step provides a broad statistical overview of all numerical features in the matrix, aiding the author in understanding global distributions, central tendencies, standard deviations, and absolute numerical ranges.
**[RESULT]** The descriptive summary explicitly shows 600 total counts for all columns. For the continuous `PRESSURE` variable, the mean is 201.02 and std is 82.81. For `SPEED`, the mean is 100.20 and std is 41.39. For the discrete `MAINTENANCE` target variable, the mean is exactly 0.34. The `COUNT_C` variable exhibits a minimum value of 6.00 and a maximum value of 68.00.
**[INSIGHT]** Because the target variable `MAINTENANCE` is strictly binary (0 or 1), a calculated mean of 0.34 directly proves that exactly 34% of the dataset records correspond to positive maintenance events. This successfully establishes the dataset's exact class balance characteristics prior to modeling.

### Exploratory Data Analysis: Correlation Analysis
**[CODE]** The code calculates a Pearson correlation matrix for numerical features by temporarily dropping the categorical-like columns `MODE` and `MAINTENANCE` (`df.drop(columns=['MODE', 'MAINTENANCE']).corr()`). It visualizes this numeric matrix utilizing an `sns.heatmap` chart. Subsequently, the `PRESSURE` column is permanently dropped from the core DataFrame using `df = df.drop(columns=['PRESSURE'])`.
**[PROCESS/CONTEXT]** The correlation matrix is utilized to gain insights into collinear numerical relationships between predictor variables. The author explicitly notes in markdown that `Speed` and `Pressure` are almost collinear, and `Speed` and `Barrel Temperature` are highly correlated. To aggressively address the extreme correlation (0.99), the author decides to completely drop the `Pressure` feature from the dataset.
**[RESULT]** The plotted heatmap visualization clearly displays a correlation coefficient of 0.99 between `SPEED` and `PRESSURE`, a coefficient of 0.85 between `SPEED` and `BARREL_TEMP`, and a coefficient of 0.86 between `PRESSURE` and `BARREL_TEMP`.
**[INSIGHT]** Permanently dropping the `PRESSURE` feature due to a severe 0.99 positive correlation with `SPEED` is an exceptionally mathematically sound decision, as extreme multicollinearity is well known to cause mathematical instability and inflated variance in coefficient estimates within linear models such as Logistic Regression. The notebook correctly identifies and explicitly mitigates this critical issue.

### Exploratory Data Analysis: Target and Categorical Features
**[CODE]** The code mathematically calculates the normalized percentage distribution of the target feature utilizing `df['MAINTENANCE'].value_counts(normalize=True)*100`. It then sequentially plots three distinct countplots: for `MAINTENANCE`, for `MODE`, and for `MAINTENANCE` explicitly grouped by the `hue` of `MODE` using `sns.countplot`.
**[PROCESS/CONTEXT]** The author examines the target feature's absolute class distribution and the discrete `MODE` feature to fundamentally understand their class composition and interaction dynamics.
**[RESULT]** The pandas value counts text output precisely shows that `MAINTENANCE` equals 0 for 66.0% of the dataset and equals 1 for 34.0% of the dataset. The countplot visualization for `MODE` indicates a perfectly equal 50/50 visual distribution (the mean of 0.50 from the prior describe output perfectly corroborates this). The grouped multi-colored countplot visually demonstrates that `MAINTENANCE` instances occur steadily across both operational modes.
**[INSIGHT]** The target dataset is observed to be moderately imbalanced (a 66/34 split). While simple accuracy is still a viable primary evaluation metric in this scenario, it is mathematically worth noting that a completely naïve baseline model that hardcodes a prediction of strictly all zeros would automatically achieve exactly 66% baseline accuracy.

### Exploratory Data Analysis: The Count_C Feature
**[CODE]** The code utilizes `sns.boxplot` to sequentially generate a vertical boxplot for `COUNT_C`, then a grouped boxplot of `COUNT_C` distributed across `MAINTENANCE` classes, and finally a highly grouped boxplot of `COUNT_C` versus `MAINTENANCE` broken down further by the `hue` of `MODE`.
**[PROCESS/CONTEXT]** The author visualizes the numeric distribution of `COUNT_C` strictly with respect to the categorical target variable to attempt to identify if it serves as a strong univariate predictor. The markdown text explicitly notes that there is a significant, observable difference between `Count_C` values across the two distinct levels of the Maintenance feature.
**[RESULT]** The generated plot charts reveal a stark, undeniable visual separation. Instances where `MAINTENANCE` is exactly 0 exhibit a `COUNT_C` boxplot distribution heavily centered around a median of approximately 20, whereas instances where `MAINTENANCE` is exactly 1 exhibit a `COUNT_C` boxplot distribution heavily centered at a median above 45. There is virtually zero visual overlap between the plotted interquartile ranges of the two classes.
**[INSIGHT]** This specific visual analysis is overwhelmingly critical to the entire project. It visually and definitively proves that the `COUNT_C` feature alone provides near-perfect linear separability of the target classification classes. This insight completely justifies the subsequent architectural decision to build machine learning models based exclusively on this single feature column.

### Exploratory Data Analysis: Scatter Plots
**[CODE]** The code utilizes `sns.scatterplot` to generate a two-dimensional scatter plot comparing `SPEED` (x-axis) versus `BARREL_TEMP` (y-axis). Then, it generates another exact copy of this scatter plot, but visually colored by the `MAINTENANCE` target feature using the `hue` parameter in seaborn.
**[PROCESS/CONTEXT]** The notebook explicitly states this analysis aims to reveal that the vast majority of required maintenance occurs exclusively at extremely high combined values of Speed and Barrel Temperature.
**[RESULT]** The initial uncolored scatter plot visually confirms a distinct positive linear correlation band between `SPEED` and `BARREL_TEMP`. The subsequently colored scatter plot unequivocally demonstrates that `MAINTENANCE = 1` (indicating maintenance is required) clusters almost entirely within the upper right-hand quadrant of the chart (representing simultaneous high speed and high temperature).
**[INSIGHT]** While `SPEED` and `BARREL_TEMP` are clearly and highly correlated with the need for `MAINTENANCE`, the immediately preceding boxplot analysis definitively established that the `COUNT_C` feature provides a much simpler, cleaner, one-dimensional numeric threshold for class separation.

### Logistic Regression: All Descriptive Features
**[CODE]** The code permanently drops the target column to construct the feature matrix `Data` and assigns the isolated target column to the variable `target`. It performs a rigorous 70/30 train-test split utilizing the function `train_test_split(Data, target, test_size=0.3, stratify=target, shuffle=True, random_state=111)`. A `LogisticRegression` classifier object is instantiated as `clf`, fit explicitly on `D_train` and `t_train`, and finally evaluated by printing the result of `.score(X=D_test, y=t_test)`.
**[PROCESS/CONTEXT]** A comprehensive baseline predictive model is established utilizing all currently available descriptive features. The explicit utilization of the `stratify=target` parameter ensures the 66/34 class balance proportion is rigorously maintained in both the training and testing array splits.
**[RESULT]** The code prints exactly `LR Test Accuracy (All Features): 0.99`.
**[INSIGHT]** The regression model achieves near-perfect predictive accuracy. However, because we already visually know that the `COUNT_C` feature perfectly separates the classification classes on its own, this extremely high accuracy using the full matrix is entirely mathematically expected and unsurprising.

### Logistic Regression: ONLY Count_C Feature
**[CODE]** The code explicitly redefines the `Data` variable to forcefully include only the singular `COUNT_C` column: `Data = df[['COUNT_C']].values`. The train-test split operation is performed again utilizing the exact same variables and identical random state. A new `LogisticRegression` model is fit on this newly reduced single-feature training set and mathematically evaluated on the newly reduced test set.
**[PROCESS/CONTEXT]** The author actively seeks to empirically prove that the isolated single feature `COUNT_C` provides precisely equivalent predictive power to the full multi-feature regression matrix.
**[RESULT]** The code prints exactly `LR Test Accuracy (ONLY Count_C Feature): 0.99`.
**[INSIGHT]** The recorded test accuracy is absolutely identical (99%). This perfectly and empirically validates the visual finding from the EDA stage indicating that `COUNT_C` is wholly sufficient to separate the binary classes, rendering the `SPEED`, `BARREL_TEMP`, and `MODE` features strictly redundant for the purpose of prediction.

### Plotting Logistic Regression Curve
**[CODE]** The code programmatically generates exactly 300 linearly spaced numeric values across the absolute min/max range of `COUNT_C` utilizing `np.linspace` and mathematically predicts their probabilities utilizing `clf.predict_proba`. A seaborn scatter plot of the actual empirical data is directly overlaid with this continuously calculated logistic probability line curve. A specific vertical line is explicitly drawn on the chart at the exact coordinate `x=33.5`.
**[PROCESS/CONTEXT]** This visual step serves to dynamically illustrate the specific mathematical decision boundary calculated and established by the single-feature logistic regression model.
**[RESULT]** The rendered plot beautifully displays the red mathematical sigmoid curve crossing the 50% (0.5) probability classification threshold at precisely the coordinate `COUNT_C = 33.5`, cleanly separating the two dense clusters of actual data points.
**[INSIGHT]** The composite visualization highly effectively demystifies the logistic regression model's internal mechanics, showing the reader exactly how the continuous floating-point probability cleanly maps to a hard, actionable decision boundary precisely at the value of 33.5.

### Decision Trees: ONLY Count_C Feature
**[CODE]** The code re-initializes the single-feature `Data` and target variables entirely, and executes the exact same train-test split function once again. A `DecisionTreeClassifier` is instantiated with the parameters `max_depth=1` and `random_state=999`. It is forcefully fit on the training data arrays and scored on the test data arrays. The tree logic is then visually rendered to the screen using `tree.plot_tree(clf, filled=True, feature_names=['COUNT_C'], class_names=['No Maintenance', 'Maintenance'])`.
**[PROCESS/CONTEXT]** The markdown text explicitly states that a full tree model was tested and achieved the exact same accuracy, so it was deliberately omitted from the final notebook. A tree with a strict depth of 1 (mathematically known as a decision stump) is utilized for maximum architectural simplicity and ultimate human interpretability.
**[RESULT]** The code prints exactly `LR Test Accuracy (ONLY Count_C Feature): 0.99` (Note: the print statement string incorrectly hardcodes "LR Test Accuracy" instead of "DT Test Accuracy"). The plotted visual tree diagram shows a single root split node executing the logic: `COUNT_C <= 33.5`, which splits into two distinct terminal leaf nodes classifying as either "No Maintenance" or "Maintenance".
**[INSIGHT]** The python code unfortunately contains a minor copy-paste string error in the print statement text ("LR Test Accuracy" instead of "DT Test Accuracy"). However, the resulting mathematical decision tree perfectly aligns with the prior logistic regression decision boundary (both models independently identified exactly 33.5 as the optimal splitting threshold). A depth-1 decision tree is essentially just an explicitly programmed logic `if` statement, representing the most absolutely parsimonious predictive model theoretically possible for this dataset.

### Decision Trees: Confusion Matrix and ROC Curve
**[CODE]** The code programmatically generates class predictions `t_pred` on the 30% test set array and calculates the exact confusion matrix using `metrics.confusion_matrix`. It calculates raw probabilities `t_prob`, computes the false positive rate, true positive rate, and calculates the absolute scalar AUC using `metrics.roc_curve` and `metrics.auc`. Finally, it plots the final ROC curve utilizing a pandas DataFrame line plot.
**[PROCESS/CONTEXT]** These granular mathematical metrics provide a significantly more detailed evaluation of the final model's performance beyond simple overall accuracy, rigorously validating the model's true positive and false positive error trade-offs.
**[RESULT]** The confusion matrix outputs a 2x2 numpy array: `[[118, 1], [1, 60]]`. The computed scalar AUC is printed exactly as `0.99`. The rendered ROC curve plot is highly square in shape, hugging the upper left corner, indicating near-perfect classification performance.
**[INSIGHT]** The internal confusion matrix reveals that there are exactly 2 misclassified instances out of the entire 180 test samples (specifically 1 false positive and 1 false negative). This equates mathematically precisely to 178/180 = 98.88% accuracy (which pandas rounds up to 99%). The massive AUC of 0.99 further mathematically confirms exceptional discriminatory power.

### Model Deployment
**[CODE]** The code prepares to train a final deployable model artifact. It re-assigns `Data` and `target` to ensure they are clean, initializes a brand new `DecisionTreeClassifier(max_depth=1, random_state=999)`, and attempts to fit it using the specific command `clf.fit(D_train, t_train)`. It serializes and writes the trained model object to disk using `joblib.dump(clf, 'DT.pkl', compress = 1)`. To actively simulate a production deployment, it loads the model back into active memory using `joblib.load('DT.pkl')`, creates a mock payload dictionary `{"COUNT_C": 17.00}`, converts it to a required 2D numpy array via `.reshape(1, -1)`, and passes it directly to the `.predict()` method.
**[PROCESS/CONTEXT]** The author explicitly states in markdown: "For model deployment, we will use the entire dataset for training of our final Decision Tree model." This entire section serves as a practical proof-of-concept for exactly how the finalized model would theoretically be utilized in a live production environment to execute predictions via an incoming JSON-style payload.
**[RESULT]** The model is successfully serialized to disk and an active prediction is executed and printed as output: `[0]`. This correctly mathematically predicts "No Maintenance" for an input value of 17.00 (which is correctly processed as less than the 33.5 tree node threshold).
**[INSIGHT]** A massive critical logical flaw exists in this final section. The author explicitly states the intent to train the final model on the "entire dataset", but the python code explicitly and erroneously executes `clf.fit(D_train, t_train)`. Because `D_train` only contains the 70% random sample split of the dataset, the final serialized production model is explicitly NOT trained on the entire dataset, directly and objectively contradicting the author's own stated markdown text intent.

## 4. Cross-Cell Dependency Analysis
- **Global Data Array Overwrites:** The fundamental variables `Data` and `target` are heavily defined in Cell 37 for the "All Features" regression model. The split array variables `D_train`, `D_test`, `t_train`, `t_test` are dynamically created in Cell 39. These exact same specific variable names are entirely and globally overwritten in Cells 44/45 for the "Count_C Only" logistic regression, and completely overwritten again in Cells 52/53 for the Decision Tree. If a user runs the notebook interactively out of strictly sequential order (e.g., trying to simply re-run the "All Features" accuracy scoring cell (41) immediately after running cell 45), the code will execute but will produce disastrously incorrect mathematical results because the global `D_test` array will have changed physical dimensional shape (dropping from 4 features down to 1). This represents a highly fragile, error-prone programming design pattern.
- **Global Classifier Overwrites:** The general model variable `clf` is repeatedly instantiated and overwritten for the full logistic regression, single-feature logistic regression, and the final decision tree. If cells are executed out of top-to-bottom sequence, the final `joblib.dump(clf, ...)` command in the deployment section could easily and inadvertently serialize a complex logistic regression model to disk instead of the intended parsimonious decision tree model.
- **Global State Mutation Accumulation:** Because identical train-test splits are physically performed three completely separate times utilizing the exact same global variables and identical random seeds, the notebook's global state is heavily mutated but manages to produce deterministic results ONLY if run strictly top-to-bottom. However, it severely violates principles of functional modularity.

## 5. Model Performance Summary
The three specific mathematical predictive models extensively evaluated in this notebook include Logistic Regression (All Features), Logistic Regression (Count_C Only), and a Decision Tree (Depth 1, Count_C Only). 
- **Logistic Regression (All Features):** Final Test Accuracy Score = 0.99
- **Logistic Regression (Count_C Only):** Final Test Accuracy Score = 0.99
- **Decision Tree (Depth 1, Count_C Only):** Final Test Accuracy Score = 0.99 (Representing exactly 178/180 correct predictions on the test set).
- **Decision Tree AUC:** Final Test Score = 0.99

The performance across all three fundamentally different models is essentially mathematically identical. The notebook highly effectively and empirically demonstrates that injecting added mathematical complexity yields absolutely zero performance gains, heavily and rationally favoring the easily interpretable decision stump model.

## 6. Conclusions and Recommendations
The notebook presents a highly effective, beautifully streamlined, and visually compelling analysis that successfully reduces a complex industrial predictive maintenance problem into a single, flawlessly interpretable rule. However, there are significant notable inconsistencies and structural code fragilities that absolutely should be addressed for production readiness.

**Observable Problems and Inconsistencies:**
1. **Critical Deployment Training Mismatch:** The markdown text in the "Model Deployment" section confidently claims the final production model is trained on the "entire dataset". However, the actual Python code executes `clf.fit(D_train, t_train)`, wrongly restricting the final training matrix to only 70% of the available data. 
2. **Confusing Copy-Paste Error in Print Statement:** In cell 55, when attempting to score the final Decision Tree model, the printed string statement text erroneously reads `LR Test Accuracy (ONLY Count_C Feature): 0.99`, confusingly labeling a decision tree execution as a Logistic Regression result.
3. **Highly Fragile Global Variables:** Repeatedly overwriting massive global arrays and model variables like `Data`, `D_train`, and `clf` across entirely different modeling paradigms makes the notebook heavily susceptible to silent, cascading mathematical logic errors if individual cells are executed out of strictly sequential order.

**Concrete Improvement Suggestions:**
1. **Fix Deployment Data:** Immediately fix the critical deployment training code to accurately reflect the stated markdown intent by running `clf.fit(Data, target)` prior to executing model serialization.
2. **Fix Typos:** Correct the misleading print statement string in the decision tree scoring cell to explicitly and correctly state "DT Test Accuracy" rather than "LR".
3. **Refactor for Modularity:** Refactor all major variable names to be strictly stage-specific (e.g., utilize `D_train_full`, `D_train_single`, `clf_lr`, `clf_dt`). This highly recommended practice will permanently remove destructive cross-cell state dependency overwriting and make the notebook completely robust for interactive, non-linear exploratory execution.
